

function DeckAdd() {
    //Add hooks for deck info: name, description etc

    //Add handlers for changing info

    //Submit handler

    return (
      //Form for creating deck. 
        
      //Submit and cancel button
    );
}

export default DeckAdd;