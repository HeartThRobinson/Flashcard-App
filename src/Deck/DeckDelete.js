import React from "react";
import { useHistory } from "react-router-dom";
import { deleteDeck } from "../utils/api/index";

function DeckDelete() {
    const history = useHistory();

    //Function to delete the deck and refresh the page.
    

    return (
      //Delete button  
    );
}

export default DeckDelete;