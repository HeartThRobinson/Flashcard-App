

function DeckStudy() {
    //Hooks

    //Load the correct deck

    return (
      //if less than three cards, display error and add cards button.

      //Display correct card and card index
      //Button for flip and next/start over.
    );
}